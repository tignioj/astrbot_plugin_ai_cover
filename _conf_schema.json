{
  "service_url": {
    "description": "RVC AI 翻唱服务地址",
    "type": "string",
    "default": "http://127.0.0.1:18888",
    "hint": "请填写 AstrBot 宿主机或容器能够访问的 RVC 服务地址。"
  },
  "api_token": {
    "description": "服务鉴权令牌",
    "type": "string",
    "default": "",
    "hint": "需要与 Windows 环境变量 AI_COVER_API_TOKEN 一致；留空表示仅依赖局域网隔离。"
  },
  "timeout_seconds": {
    "description": "单次翻唱超时秒数",
    "type": "int",
    "default": 3600
  },
  "default_model": {
    "description": "默认翻唱模型",
    "type": "string",
    "default": "芙宁娜",
    "hint": "发送 /翻唱 或 /翻唱下载 时未指定模型，就使用这里的模型名称。"
  },
  "index_rate": {
    "description": "RVC 特征索引占比",
    "type": "float",
    "default": 0.75,
    "slider": {"min": 0, "max": 1, "step": 0.05}
  },
  "rms_mix_rate": {
    "description": "输入与输出包络融合比例",
    "type": "float",
    "default": 0.25,
    "slider": {"min": 0, "max": 1, "step": 0.05}
  },
  "protect": {
    "description": "清辅音保护",
    "type": "float",
    "default": 0.25,
    "slider": {"min": 0, "max": 0.5, "step": 0.01}
  },
  "vocal_gain": {
    "description": "默认翻唱人声音量倍率",
    "type": "float",
    "default": 1.0,
    "slider": {"min": 0, "max": 2, "step": 0.05},
    "hint": "1.0 为原音量，0 为静音，2.0 为两倍；命令中传入音量时会临时覆盖此值。"
  },
  "instrumental_gain": {
    "description": "默认伴奏音量倍率",
    "type": "float",
    "default": 1.0,
    "slider": {"min": 0, "max": 2, "step": 0.05},
    "hint": "1.0 为原音量，0 为静音，2.0 为两倍；命令中传入音量时会临时覆盖此值。"
  },
  "send_as_record": {
    "description": "将结果作为语音消息发送",
    "type": "bool",
    "default": false,
    "hint": "关闭时发送 MP3 文件；开启时发送平台语音消息。插件会保留 MP3 格式交给消息适配器，避免在 AstrBot 中转为体积更大的 WAV；平台仍可能再次转码或限制超长音频。"
  },
  "output_retention_hours": {
    "description": "AstrBot 结果文件保留小时数",
    "type": "int",
    "default": 24
  }
}
